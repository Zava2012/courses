(function(window) {
    window["env"] = window["env"] || {};

    // Environment variables
    window["env"]["apiHost"] = "${BACKEND_HOST}";
    window["env"]["apiPort"] = "${BACKEND_PORT}";
    window["env"]["production"] = "${PROD_STATE}";
  })(this);
